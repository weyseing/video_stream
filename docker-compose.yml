services:
  video_stream:
    build: .
    container_name: video_stream
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - "8000:8000"
      - "8001:80"
    networks:
      global-net:

networks:
  global-net:
    external: true